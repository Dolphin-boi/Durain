หากจะ run back

1. run terminal และ set current directory ให้ตรงกับ folder back
2. กรณีไม่มี venv รัน :python3 -m venv .venv
3. ต่อด้วย :pip install -r requirements.txt
4. เรียกใช้งาน venv โดย   -window :.venv\Scripts\Activate.ps1
                        -Mac    :source .venv/bin/activate (ไม่เคยลอง)
5. python app.py

port 5000